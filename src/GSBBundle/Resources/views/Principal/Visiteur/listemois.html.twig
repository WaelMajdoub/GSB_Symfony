<h2>Mes fiches de frais</h2>
<div class="row">
    <div class="col-md-4">
        <h3>Sélectionner un mois : </h3>
    </div>
    <div class="col-md-4 selectMois">
        {{ form_start(formMois) }}
        <fieldset>
            <div class="form-group">
                {{ form_widget(formMois.date) }}
            </div>
            {{ form_widget(formMois.ajouter) }}
            {{ form_widget(formMois.effacer) }}
        </fieldset>
        {{ form_end(formMois) }}
    </div>
</div>