{% extends "@GSB/Default/base.html.twig" %}
{% block body %}
    {{ include('@GSB/Default/head.html.twig') }}
    {{ include('@GSB/Principal/Visiteur/listemois.html.twig') }}

    <hr>
    <div class="panel panel-primary">
        <div class="panel-heading">Fiche de frais du mois : {{ ficheFrais.mois |slice(4,6)}}-{{ ficheFrais.mois |slice(0,4) }} </div>
        <div class="panel-body">
            <strong><u>Etat :</u></strong> {{ ficheFrais.idEtat.libelle }} depuis le {{ ficheFrais.dateModif|date("d/m/Y") }} <br>
            <strong><u>Montant validé :</u></strong> {{ ficheFrais.montantValide }} €
        </div>
    </div>


    {{ dump(ficheFrais) }}

    <div class="panel panel-info">
    <div class="panel-heading">Eléments forfaitisés</div>
    <table class="table table-bordered table-responsive">
        <tr>
            <?php
            foreach ($lesFraisForfait as $unFraisForfait) {
                $libelle = $unFraisForfait['libelle'];
                ?>
            <th> <?php echo htmlspecialchars($libelle) ?></th>
            <?php
            }
            ?>
        </tr>
        <tr>
            <?php
            foreach ($lesFraisForfait as $unFraisForfait) {
                $quantite = $unFraisForfait['quantite'];
                ?>
            <td class="qteForfait"><?php echo $quantite ?> </td>
            <?php
            }
            ?>
        </tr>
    </table>
    </div>
    <div class="panel panel-info">
        <div class="panel-heading">Descriptif des éléments hors forfait - <?php echo $nbJustificatifs ?> justificatifs reçus</div>
        <table class="table table-bordered table-responsive">
            <tr>
                <th class="date">Date</th>
                <th class="libelle">Libellé</th>
                <th class='montant'>Montant</th>
            </tr>
            <?php
        foreach ($lesFraisHorsForfait as $unFraisHorsForfait) {
            $date = $unFraisHorsForfait['date'];
            $libelle = htmlspecialchars($unFraisHorsForfait['libelle']);
            $montant = $unFraisHorsForfait['montant'];
            ?>
            <tr>
                <td><?php echo $date ?></td>
                <td><?php echo $libelle ?></td>
                <td><?php echo $montant ?></td>
            </tr>
            <?php
        }
        ?>
        </table>
    </div>
{% endblock %}